import React from 'react';

const ProjectDiscription = () => {
    return (
        <div className='ProjectDiscription'>
            <p> Using React Redux toolkit </p>
            <p> Get data and install json-server: <code> npm i json-server </code>  </p>
            <p> or  <code> npm i json-server -g </code>  </p>
            <p> To start server json-server Type in CLI with: <code> $ json-server --watch data/db.json --port 3500 </code> </p>
         
        </div>
    );
};

export default ProjectDiscription;